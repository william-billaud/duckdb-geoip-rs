# name: test/sql/rusty_quack.test
# description: test rusty_quack extension
# group: [geoip-rs]

# Before we load the extension, this will fail
statement error
SELECT geoip_asn_org('Sam');
----
Catalog Error: Scalar Function with name geoip_asn_org does not exist!

# Require statement will ensure the extension is loaded from now on
require duckdb_geoip_rs

require icu

# Confirm the extension works (ASN)
# Use maxmindb test db, fromm test_db directory
query TT
SELECT geoip_asn_org('83.206.12.5'), geoip_asn_num('83.206.12.5');
----
France Telecom - Orange
3215

# Confirm the extension works (City)
query TT
SELECT geoip_country_iso('2.125.160.217'),geoip_city('2.125.160.217');
----
GB
Boxford


# Confirm the extension works (ASN) /IPV6
query TT
SELECT geoip_asn_org('2a01:c000::fade'), geoip_asn_num('2a01:c000::fade');
----
Orange S.A.
5511

# Confirm the extension works (City/IPv6)
query TT
SELECT geoip_country_iso('2a02:da40::6'),geoip_city('2a02:da40::6');
----
GB
(empty)

# Confirm empty result on non ip query
query TTTT
SELECT geoip_asn_org('not_an_ip'),geoip_asn_num('not_an_ip'), geoip_city('not_an_ip'),geoip_country_iso('not_an_ip');
----
(empty)
(empty)
(empty)
(empty)

statement error
SELECT geoip_asn_org(1);
----
Binder Error: No function matches the given name and argument types 'geoip_asn_org(INTEGER_LITERAL)'. You might need to add explicit type casts.

statement error
SELECT geoip_asn_num(1);
----
Binder Error: No function matches the given name and argument types 'geoip_asn_num(INTEGER_LITERAL)'. You might need to add explicit type casts.

statement error
SELECT geoip_city(1);
----
Binder Error: No function matches the given name and argument types 'geoip_city(INTEGER_LITERAL)'. You might need to add explicit type casts.

statement error
SELECT geoip_country_iso(1);
----
Binder Error: No function matches the given name and argument types 'geoip_country_iso(INTEGER_LITERAL)'. You might need to add explicit type casts.


# Test documentation
query TTTTTT
SELECT database_name, function_name, parameter_types, return_type, description, examples  FROM duckdb_functions() WHERE function_name = 'geoip_asn_org';
----
system
geoip_asn_org
[VARCHAR]
VARCHAR
NULL
[]